#!/usr/bin/node
// an empty class Rectangle that defines a rectangle

module.exports = class Rectangle {
  constructor (w, h) {
    if (width > 0 && height > 0) {
      this.width = w;
      this.height = h;
    }
  }
};
